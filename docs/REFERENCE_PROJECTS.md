# peterMath Reference Projects

Use external repositories only as narrow references. The current competition product is a Lenia teaching game, so outside code should not change the repository root, add extra simulation worlds, or turn the app back into a technical demo.

## Primary Reference: Lenia

Lenia remains the conceptual foundation because it is continuous, rule-driven, visually organic, parameter-driven, and teachable through short missions.

Useful ideas to study:

- deterministic species/preset management;
- radial kernels and growth functions;
- compact explanations for local rule -> global behavior;
- high-quality continuous-field rendering;
- save/share state formats.

## UI References

Look for examples of short interactive learning loops:

- one visible objective;
- immediate feedback;
- optional hint;
- explanation unlocked after action;
- export/share as the final proof step.

## Local Reference: PeterHaipi

`/Users/sonics/project/PeterHaipi` is the primary local reference for the next delivery pass. Use it for interaction and submission patterns only:

- clear first path: `1 选工具 -> 2 选任务 -> 3 点画布`;
- canvas coachmark, hover/interaction preview, and click feedback;
- mission cards that show status, recommended action, and completion;
- math narration that unlocks after play;
- judge-safe package structure with native/main entry, fallback entry, screenshots, video, data, and final checklist.

Do not copy PeterHaipi's Conway Life content, project identity, package names, or simulation model. peterMath remains Lenia-only.

## Rule

Do not import large architectures blindly. Preserve `peterMath` naming, Rust/egui delivery, Windows artifact flow, deterministic seeds, Lenia presets, mission mode, and evidence exports. Copy interaction/submission patterns, not Conway Life content or product identity.
